# 2026-07-20 工作日志

## 导入作品集项目到当前工作区
- 来源：桌面 `portfolio-export-2026-07-20.zip`（另一台设备 WorkBuddy 导出的工作区，含 React+Vite 作品集源码 + `.workbuddy/memory` 日志）。
- 复制 `src/ public/ index.html package.json package-lock.json vite.config.js .gitignore` 到工作区根，**未复制导出里的 `.workbuddy`**（避免覆盖本会话记忆）。
- 依赖：用本机隔离 node `C:\Users\PC\.workbuddy\binaries\node\versions\22.22.2` 跑 `npm install`（68 包，22s）。
- 运行：dev server 后台跑在 `http://localhost:5173/`（task 7hpNh8）；`npm run build` 通过（118 模块，3.2s）。

## 修正遗留命名不一致
- 站点名上次已改「张帆」，但 `index.html` `<title>` 仍写「林知」、`favicon` 字母为 `L`、旧紫 `#8a7dff`。
- 已改：title→「张帆 · 视觉与 AI 影像作品集」，favicon 字母 `L`→`Z`、配色→主色红 `#ff4a4a`。
- HMR 已生效，curl 验证 title 正确。

## 待与用户确认的占位遗留（导出日志也标注"占位未动"）
- `src/data/content.js` 邮箱仍为 `hello@linzhi.studio`（linzhi=林知，与张帆不一致），需用户给真实邮箱/微信/城市。
- 项目媒体 `projects[].media` 全是 `{type:'placeholder',hue:...}`，无真实作品图/视频；`public/` 仅有 `hero.mp4`/`hero2.mp4` + `ASSETS-README.md`，缺 `avatar.jpg` 与 `public/projects/*`。
- 下一步方向待用户定：填真实内容 / 改视觉细节 / 调整 SpecularCard 红色深浅与常驻动画。

## 19:00 — 把 About 第二屏个人信息改为简历内容
- 用户给两张截图：第一张当前 About（左头像+右 intro/联系/统计），第二张简历（教育、技能、工作经验）。
- 改 `content.js` 的 `about`：从占位文案/统计改为简历结构 `fullName/englishName/resume.education/skills/experience`。
- 重写 `About.jsx`：右侧渲染简历三卡片，移除旧 intro、联系、统计。
- `global.css` 新增 `.resume__*` 样式，并把头像光晕紫改红。

## 19:13 — 调整 About 左右平衡（左列固定个人卡片）
- 用户反馈简历堆右边让左侧形象图显得很矮。
- 重构 `About.jsx`：左侧 `<aside class="about__media">` 改为头像 + 姓名(张帆/Zhang Fan) + 身份(site.role) 固定个人卡片；右侧只留 `.resume` 三区块（移除右侧姓名头部）。
- `global.css`：`.about__media` 改 `position:sticky; top:120px; align-self:stretch; justify-content:center`（左列撑满高度、内容垂直居中、滚动时头像常驻）；`.avatar` 加 `width:100%`；新增 `.about__media-info/-name/-en/-role`；删除无用 `.resume__header/.resume__name/...`；移动端 `@media(max-width:900px)` 下 `.about__media` 改回 `static`。
- 验证：`npm run build` 通过（118 模块）。

## 19:20 — 左列姓名与头像位置互换
- 用户建议把名字块放到头像上方更协调。
- `About.jsx` 中交换 `.about__media-info` 与 `.avatar` 的 DOM 顺序，现在左侧从上到下：姓名（张帆 / ZHANG FAN）→ 身份 → 头像。
- `npm run build` 通过。

## 19:31 — 第一个项目卡片加入真实视频《霓虹之城》
- 源文件：`C:\Users\PC\Desktop\霓虹之城\霓虹之城（字幕版）.mp4`，479MB，H.264(yuv420p,2560x1440,30fps)+AAC，时长≈3:29。
- 处理：因中文路径 MSYS 传给 Windows ffmpeg 会乱码，先 `cp` 成 ASCII 临时名；再用 imageio 自带 ffmpeg（`.../imageio_ffmpeg/binaries/ffmpeg-win-x86_64-v7.1.exe`）`-c copy -movflags +faststart` 无损前置 moov，输出 `public/projects/neon-city.mp4`（457MB）。删临时源。
- 注意：本机无系统 ffmpeg；可用 ffmpeg 在 `C:\Users\PC\.workbuddy\binaries\python\...\imageio_ffmpeg\binaries\ffmpeg-win-x86_64-v7.1.exe`。
- `content.js` `projects[0]`(p1)：`media` 改 `{type:'video', src:'/projects/neon-city.mp4'}`，标题改「霓虹之城 — AI 科幻短片」，描述贴合。
- 验证：Vite dev 对 `/projects/neon-city.mp4` 支持 `Accept-Ranges`(206)，浏览器可流式播放；`npm run build` 通过。
- **待定**：视频 457MB 对网页自动循环偏重，已向用户提议可压成网页版（≈20–40MB）作为可选下一步。

## 19:36 — 第一个视频卡片改为「点开再看」
- 用户要求：点击才播放、带声音、可拖动进度（不再 autoPlay+muted+loop）。
- 抽封面：`ffmpeg -ss 5 -frames:v 1 -vf scale=1280:-2 -q:v 4` → `public/projects/neon-city-poster.jpg`（34KB）。
- `content.js` p1 media 增加 `poster:'/projects/neon-city-poster.jpg'`。
- 重写 `Projects.jsx`：拆出 `ProjectCard` 组件管理 `playing` 状态与 `videoRef`；视频默认 `preload="metadata"`+poster，居中红色脉冲播放按钮覆盖层；点击 → `muted=false; controls=true; play()` + `playing=true`；`onEnded` 复位；播放时移除 `parallax` 类避免控件抖动；视频卡片 `<a>` 点击 `preventDefault` 防止跳转。
- `global.css` 新增 `.project__playbtn/-icon` 及 `@keyframes playbtn-pulse`（红色光晕脉冲，hover 放大静止）。
- 验证：`npm run build` 通过；poster/video 均 200；dev 已热更新。

## 19:43 — 修复：点播放按钮跳到第三屏顶部
- 现象：点视频播放按钮后页面跳到 `#work`（精选项目区/第三屏）顶部。根因：播放按钮位于 `<a href="#work">` 内，点击触发锚点跳转。
- 修复：`Projects.jsx` 中无真实外链(`p.link`)的项目卡片改用 `<div class="project__card">` 而非 `<a>`，有真实外链才渲染 `<a target="_blank">`；删除对 `#work` 跳转的依赖。播放按钮的 preventDefault/stopPropagation 保留。
- 验证：`grep '#work'` 在 Projects.jsx 为 0；`npm run build` 通过。

## 19:48 — 视频压成网页版（neon-city.web.mp4）
- 用户要求把 457MB 原片压成网页版。ffmpeg 两遍编码：`libx264 -preset slow -pix_fmt yuv420p -b:v 1200k -maxrate 1500k -bufsize 2400k -vf scale=1920:1080` + 音频 `aac -b:a 128k -movflags +faststart`。
- 坑：本环境无 `/dev/null`，第一遍 `-pass 1 -f mp4 /dev/null` 会失败；改输出到临时文件 `.pass1.mp4`，第二遍再写到正式文件。
- 输出 `public/projects/neon-city.web.mp4` ≈ **33.5MB**（34998576 字节），时长/画质保持。原片 `neon-city.mp4`(457MB) 保留作存档。
- 验证：dev 对 `neon-city.web.mp4` 返回 `206 Accept-Ranges`（可拖动进度）；`npm run build` 通过（3.62s）。

## 19:50 — 视频从 p1 移到 p3
- 用户要求「把这个视频放到 p3」。把 `neon-city.web.mp4`（点开播放）从 `projects[0]`(p1) 移到 `projects[2]`(p3)。
- p3 现为「霓虹之城 — AI 科幻短片」(视频卡)，p1 还原为占位「Neon Drift — 品牌动态影像」(hue violet)，p2 不变。
- 验证：`npm run build` 通过；dev 已热更新。

## 19:56 — 去掉项目卡片左上角 category 标签
- 用户截图指出卡片左上角「AI 视频 · 叙事」标签想删除。
- 删除 `Projects.jsx` 中的 `<span className="project__badge">{p.category}</span>`。
- 删除 `global.css` 中 `.project__badge` 全部样式。
- 验证：`grep 'project__badge'` 在 Projects.jsx 为 0；`npm run build` 通过。

## 20:29 — 视频封面改回第一帧（除 p2 外）
- 用户要求：除 p2（月栖旅馆，用自带封面 `moon-inn-ep2-poster.jpg`）外，其余 6 个视频封面都改用**第一帧**（原抽的是第 5 秒）。
- 用 ffmpeg `-ss 0 -frames:v 1 -vf scale=1280:-2 -q:v 4` 覆盖重抽：god-falls-ep1 / neon-city / tiandao / secret-heir / dual-fight / spring-che 的 `-poster.jpg`。
- p2 封面未动（保留用户自带图，334KB）。
- 验证：dev 对 7 张 poster 均返回 200 + image/jpeg + 真实 Content-Length；无需改代码，dev 静态资源热更新。

## 20:30 — 去掉项目卡片左下角「年份 · 客户」元信息
- 用户截图要求去掉卡片左下角的 `2026 · 个人创作`。
- 删除 `Projects.jsx` 中 `.project__meta` 的 DOM（year + dot + client）。
- 删除 `global.css` 中 `.project__meta` 及 `.project__meta .dot` 全部样式。
- 验证：`grep 'project__meta'` 在 JSX 与 CSS 中均为 0；`npm run build` 通过。

## 20:33 — 项目卡片文字区增加左侧内边距
- 用户截图：卡片左下角文字「太靠边」。
- 将 `.project__info` 从 `padding: 8px 0` 改为 `padding: 8px 16px`，让标题与描述左右都留 16px 内边距，不再紧贴卡片左边缘。
- 验证：`npm run build` 通过。

## 项目屏底部新增 TVC 自动滚动条
- 新增 `src/components/TvcMarquee.jsx`：底部横向无缝滚动卡片条，复制一遍内容做 `-50%` 关键帧循环，悬停暂停、播放视频时暂停动画。
- `src/data/content.js` 新增 `tvc` 数组（6 个占位项，待用户填真实 TVC 视频：`media.src` 放视频、`media.poster` 放封面、`title/brand/year` 填文案）。
- `Projects.jsx` 在 `projects__list` 后渲染 `<TvcMarquee />`；`global.css` 新增 `.tvc*` 样式（暗色卡片 340px、16:9、红橙进度条、左右渐隐遮罩）。
- build 通过（4.75s）。

## 填充 6 个真实 TVC 视频到滚动条
- 来源桌面 6 个源：DIOR、KVIDIO、狼蛛鼠标(AULA)、xiaomi17Ultra、瑞幸咖啡、纯福狗粮。
- 用 imageio_ffmpeg 的 ffmpeg 两遍 libx264（preset medium, 1500k/2000k, scale min(1920,iw):-2, aac 128k, faststart）压成网页版；各片 passlogfile 独立防冲突。
- 中文路径先 cp 为 ASCII 临时名后处理；封面取各片 1s 处截图（q:v2）。
- 输出 public/projects/{dior,kvidio,aula,xiaomi,luckin,chunfu}.mp4 + -poster.jpg（3–4.5MB/个）。
- content.js `tvc` 数组 6 项全部替换为真实标题/品牌/年代；build 通过(5.24s)。

## 精简 TVC 滚动卡片：只保留大标题
- 用户反馈底部滚动卡不需要品牌/年份小字，只保留大标题。
- 修改 `TvcMarquee.jsx`：移除了 `tvc__brand` 行；清理 `global.css` 中对应的 `.tvc__brand` 样式；build 通过(4.81s)。

## 移除 TVC 区标题左侧 eyebrow 小字
- 用户反馈「商业广告 · TVC」标题左边「03 — TVC / 广告」小字不需要。
- 修改 `TvcMarquee.jsx`：删除 `<p className="eyebrow">03 — TVC / 广告</p>`；build 通过(4.86s)。

## 在 P7 下新增 P8 占位卡片
- 用户要求 P7 下再加一个 P8。在 content.js projects 数组追加 p8（index 08，标题「作品 08」，media.src 空 → 显示「AI 视频 · 占位」占位框，desc 暂写「（待填充）」）。
- build 通过(4.97s)。待用户提供 P8 真实视频/标题。

## P8 填充《空城里》IP 动画
- 来源桌面 `空城里/IP动画.mp4`，ffmpeg 两遍 libx264(preset slow) 压成 `public/projects/ip.mp4`(6.0MB)，封面 `ip-poster.jpg`(1s 截图)。
- content.js p8 改为：title「空城里」、category「AI 视频 · IP 动画」、desc 原创 IP 动画描述；build 通过(5.07s)。

## P1 描述替换为暗黑史诗长文案
- 用户指定 P1《When the God Falls》desc 改为一段末世神话世界观长文案；build 通过(4.77s)。

## 「优势」屏改为「电商套系图展示屏」
- 用户要求把 Strengths 屏改成电商套系图展示。删除 `src/components/Strengths.jsx`，新建 `Ecommerce.jsx`（id=ecommerce，eyebrow 03，标题「电商套系图」）。
- content.js：`strengths` 数组替换为 `ecommerce` 数组（6 个占位套系：美妆/食品/3C/服饰/家居/母婴，每项含 tag + images[]，留空显示「套系图·待上传」占位，images 多张时角标显示张数）。
- App.jsx 改用 <Ecommerce/>；nav 标签「优势」→「电商套系」(#ecommerce)；contact eyebrow 由 05 改为 04。
- global.css 移除 strengths 样式，新增 `.ecards__grid/.ecard*` 样式（3/2/1 列响应式，4:5 封面，悬停缩放）。
- build 通过(5.07s)。待用户上传真实电商套系图（放 public/projects/ 并填 images）。

## 电商套系图屏改为「卡组 + 灯箱」样式
- 用户要求每张卡只展示缩略图（封面），点击打开灯箱看整套套系图。
- `Ecommerce.jsx`：`.ecard` 改为 `<button>`，加 `active` state 控制灯箱；hover 显示覆盖层（标题 + 「查看完整套系 →」）；灯箱含头部（tag/标题/张数/关闭）、body 网格（3 列，多图平铺）、Esc/点遮罩关闭、打开时锁 body 滚动。
- `global.css`：`.ecard` 加按钮重置；移除 `.ecard__info` 底标题块，新增 `.ecard__overlay*` 悬停层 + `.esuite-modal*`（固定全屏、模糊背景、面板淡入/弹入、3 列图组、空态「该套系图尚未上传」、720px 下 2 列）。
- build 通过(5.36s)。images 仍为空（占位），待用户上传真实套系图。

## 灯箱改为单图轮播
- 用户要求灯箱里单独轮流展示每张图，点切换按钮看下一张。
- `Ecommerce.jsx`：`active` 套系外加 `idx` 图片序号 state（打开新套系归零）；body 改单图 viewer，加左右 `.esuite-nav` 切换按钮（循环）+ 底部 `.esuite-dots` 圆点指示；支持键盘 ←/→ 切换、Esc 关闭；头部显示「第 X / N 张」。
- `global.css`：删除 `.esuite-modal__grid/__cell` 旧网格样式，新增 `.esuite-modal__stage/__viewer/__nav/__dots/__dot` 单图轮播样式（图 contain 显示、淡入、箭头圆形按钮、激活圆点放大渐变）。
- build 通过(5.29s)。

## 填充第一个电商套系（10 张 KVIDIO 3C 图）
- 用户发了 10 张 KVIDIO 耳机电商图（主图1-5、海报1-2、banner1-2、详情图），要求按顺序放到第一个套系。
- 图片复制到 `public/projects/e1-01.png`～`e1-10.jpg`；同时把 e1 标题从「美妆护肤套系」改为「KVIDIO 3C 套系」、标签改为「3C」，images 数组按顺序填入 10 条路径。
- 灯箱内会显示「第 1 / 10 张」，可左右切换/点圆点切换。build 通过(5.50s)。

## 修复灯箱被下一屏大标题透穿的问题
- 用户反馈打开灯箱后能看到下一屏「CONTACT」大标题；原因是模态背景 `rgba(4,4,7,0.82)` 太透。
- 将 `.esuite-modal` 背景改为 `rgba(7,7,10,0.96)`、z-index 由 200 提升至 9999、blur 加大到 14px。build 通过(5.24s)。

## 再次修复灯箱被下一屏标题穿透
- 用户反馈上版 0.96 半透明背景仍能看到「CONTACT」大标题；将 `.esuite-modal` 背景改为纯色 `#07070a`（完全不透明）并移除 blur，彻底解决透穿。build 通过(4.70s)。

## 用 Portal 把灯箱挂载到 body 根节点
- 前两版纯色背景仍被下一屏标题穿透，根本原因是 `.section` 都是 z-index:1 的 stacking context，modal 被困在 `.ecommerce` 内部，而同级的 `.contact` 因 DOM 在后而覆盖其上。
- 将 `Ecommerce.jsx` 中的灯箱改为 `createPortal(..., document.body)`，使其脱离 `.ecommerce` 的 stacking context，z-index:9999 在根层级生效，真正覆盖 nav 与 contact。build 通过(4.76s)。

## 灯箱超长竖图（详情长图）优化展示
- 用户要求把最后一张详情长图(e1-10.jpg)按之前提的方案改：不再被压成细条。
- `Ecommerce.jsx`：新增 `tall` state；img 加 `onLoad`，按 `naturalHeight/naturalWidth > 1.6` 判定超长竖图，给 viewer 加 `.is-tall` 类；切换图(idx/active 变化)时重置 tall 待重判。
- `global.css`：新增 `.esuite-modal__viewer.is-tall` 整宽铺满(max-width min(74vw,560px))、竖向滚动(max-height 76vh, overflow-y auto)、顶部对齐 + 自定义滚动条，img `max-height:none`。
- build 通过(4.83s)。普通方图/竖图(高宽比≤1.6)仍走原 contain 模式。

## 灯箱图片改为滚轮缩放 + 左键拖动
- 用户反馈超长详情图在灯箱里还是太小，要求鼠标滚轮缩放、左键拖动查看。
- `Ecommerce.jsx`：新增 `view{scale,x,y}`、`dragging`、`viewerRef/imgRef/dragRef`；`clampView()` 按容器/图片尺寸限制平移范围防拖出；用原生非 passive `wheel` 监听(depend on active,tall)实现 1–5 倍缩放；viewer 上加 onMouseDown/Move/Up/Leave 实现左键拖动平移(未缩放且非长图不触发)；img 加 `draggable=false` 与内联 transform；onLoad 仍判 tall 并重置 view；tall 时头部显示「滚轮缩放·拖动查看」。
- `global.css`：`.esuite-modal__img` 加 `cursor:zoom-in; user-select:none; -webkit-user-drag:none`；`.is-tall` 改为 overflow:hidden + `cursor:grab`，`.is-grabbing` 改 grabbing；移除旧的竖向滚动条样式。普通图亦支持缩放拖动。build 通过(4.69s)。

## 放开灯箱缩放范围（放大+缩小均不卡死）
- 用户反馈缩放被固定框限制，要求既能放大也能缩小。
- `Ecommerce.jsx`：滚轮缩放范围由 [1,5] 改为 [0.2, 10]（可缩小到 0.2×、放大到 10×）；onMouseDown 拖拽触发条件由 `scale<=1 && !tall` 改为 `scale===1 && !tall`，使缩小或放大时均可拖动平移。clampView 平移边界保留（防拖出视野）。build 通过(5.01s)。

## 灯箱初始图片尺寸收敛到标题下白线以内
- 用户指出灯箱标题下方的白色分隔线为「第二根白线」,要求每张图初始形态不超过此线。
- `global.css`: `.esuite-modal__img` max-height 由 68vh 改为 48vh, max-width 由 100% 改为 75%; `.esuite-modal__img.is-tall` max-height 由 none 改为 48vh, max-width 由 min(74vw,560px) 改为 min(52vw,420px),保证初始状态不顶到标题下白线,缩小时更居中、留足边距; zoom 后仍可用滚轮放大/拖动查看。build 通过(4.90s)。

## 移除放大后的平移边框限制
- 用户要求放大时不要有框限制。修改 `Ecommerce.jsx` 的 `clampView`：scale<=1 时仍居中(0,0)，scale>1 时直接返回传入的 x/y，不再按容器尺寸夹取，实现放大后自由拖动、无视野边框约束。build 通过(5.52s)。

## 移除灯箱图片外框白边
- 用户反馈灯箱图片放大后有明显「框」（白边）。查看 `.esuite-modal__img` 带 `border: 1px solid var(--border)` 和 `border-radius: 12px`，构成可见外框。
- 删除 `.esuite-modal__img` 与 `.esuite-modal__img.is-tall` 的 border 与 border-radius，仅保留缩放/拖动效果，build 通过(5.14s)。

## 修复竖图(#6海报1/#10详情图)灯箱「框」
- 这两张是 is-tall 超长竖图，旧样式 `.is-tall` 把图限在 `min(52vw,420px)` 窄列居中，浅色背景在黑底上形成一圈「框」。
- 改为 `.esuite-modal__img.is-tall { width:100%; height:auto; max-width:none; max-height:none }`，让图撑满查看器宽度、浅色背景铺到边缘，消除黑底框；overflow:hidden 仍裁切超长部分，缩放/拖动不变。build 通过(4.99s)。
- 备注：#10 原图仅 157px 宽，撑满会被放大变糊，已提示用户可换更高清源。

## 灯箱竖图查看器撑满宽度
- 用户反馈第 6/10 张在灯箱里仍只能缩成窄条显示，不像其它图那样放大铺满。原因是 `.esuite-modal__viewer.is-tall` 自身 `width:auto`，导致子 img 的 `width:100%` 失效（父级按内容宽度收缩）。
- 给 `.esuite-modal__viewer.is-tall` 补 `width: 100%`，使其占满 stage 宽度，img 随之撑满，消除两侧黑边。build 通过(5.17s)。

## 调整第6张初始形态=第7张
- 用户反映第6张(e1-06.jpg, 941x1672, ratio 1.78)初始在灯箱里太大，希望跟第7张(e1-07.jpg, 1006x1563, ratio 1.55)一样。
- 原因：is_tall 阈值 1.6 把 1.78 的 e1-06 判成超长竖图(撑满整宽)，而 1.55 的 e1-07 走普通受限尺寸。
- 将 `Ecommerce.jsx` 中 is_tall 阈值由 1.6 改为 2.5：仅真正超长图(如 e1-10 ratio 12.23)撑满，海报类(1.5~1.8)按普通受限尺寸显示，与第7张一致。build 通过(5.26s)。

## 修复第10张(详情长图)拖不动 + 模糊
- 用户反映 e1-10.jpg 拖不动且模糊。
- 拖不动根因：`clampView` 改为「scale<=1 强制归零」后，长竖图在 scale=1 时 onMouseMove 被归零，无法平移。给 `clampView` 加 isTall 参数：仅「scale<=1 且非长图」才归零；长图在 scale=1 也允许自由拖动。wheel/onMouseMove 调用处传入 tall。
- 模糊根因：e1-10 原图仅 157px 宽，之前 is-tall 强制 width:100%(≈1000px) 放大6倍。改为 `.esuite-modal__img.is-tall { width:auto; max-width: min(440px,82%); max-height:none }` 限制过度放大(约2.8倍)。但仍受限于157px源分辨率，真正清晰需用户提供更高清源。
- build 通过(5.39s)。

## 替换 e1-10 并调整长图初始宽度=第6张
- 用户提供新详情图，但 WorkBuddy 落地的本地文件仍为 157x1920（和原 e1-10 同尺寸），未真正高清。
- 已替换 `public/projects/e1-10.jpg` 为该文件；按用户要求把 `.esuite-modal__img.is-tall` 的初始 max-width 从 440px 改为 `min(27vh, 82%)`，与第6张(e1-06 941x1672, ratio 1.78)在正常模式(max-height:48vh)下的显示宽度(48vh/1.78≈27vh)一致。
- 如需真正清晰，需用户提供宽度远大于 157px 的高清源。build 通过(5.11s)。

## 接入高清详情页原文件 e1-10.png
- 用户从 D:/作品1/详情页.png 发原文件，尺寸 790x9618（之前剪贴板版只有 157px 宽）。
- 复制到 `public/projects/e1-10.png`；更新 content.js e1 images 第10项为 `/projects/e1-10.png`；删除旧的 e1-10.jpg。
- 结合上一版改动，长图初始宽度仍为 `min(27vh, 82%)`（与第6张一致），但源图 790px 宽，按 27vh 显示属于 downscale，清晰不模糊。build 通过(5.06s)。

## 第10张灯箱初始形态按截图调整
- 用户提供截图，要求第10张长图初始宽度更宽、顶部对齐标题下方白线。
- `Ecommerce.jsx`：给 `.esuite-modal__stage` 加 `is-tall` 类（与 img 的 tall 状态同步）。
- `global.css`：`.esuite-modal__img.is-tall` max-width 从 `min(27vh,82%)` 改为 `min(720px, 82%)`（与截图宽度接近，且 720<790 属 downscale，清晰）；新增 `.esuite-modal__stage.is-tall { padding-top:0; align-items:flex-start }`，使长图顶部紧贴 header 底部分隔线。
- build 通过(5.66s)。

## 性能审查 + 视频恢复事故处理（后续会话）
- 用户要求审查性能（视频/图片体积、WebGL 背景、滚动监听、GSAP、首屏加载）并直接优化。
- 已确认早前已落地的优化：index.html 字体 async(media="print")、Hero 第二视频 preload=metadata、SpecularCard dpr≤2 + reduced-motion 静态、PixelBlast dpr≤1.5 + visibilitychange 暂停 + reduced-motion 静态、vite manualChunks 分包(three/react/gsap/ogl 独立 chunk)。
- 事故：早前批量压缩脚本把 3 个项目视频覆盖成 0 字节（moon-inn-ep2.mp4、secret-heir.mp4、neon-city.web.mp4），已删 0 字节文件，卡片回退 poster。
- 根因定位（关键）：① 后台 Bash(run_in_background) 中 `$FF` 变量未展开→ffmpeg 退出 127→空 tmp 被 `mv` 覆盖源；② 本沙箱**无 `/dev/null`**，`-pass 1 -f mp4 /dev/null` 直接失败（ratecontrol 读不到 stats）。安全写法：直接 Bash + 内联 ffmpeg 绝对路径 + pass1 输出到真实临时文件 + 校验 `[ -s "$TMP" ]` 非空才 `mv`。
- 恢复：neon-city.web.mp4 从微信母片（`C:/Users/PC/Documents/xwechat_files/wxid_rebd8c7g8yik21_11a0/msg/video/2026-07/c7be4be8483d46ab19d8165b06639059_raw.mp4`，457MB，1440p，3:29）两遍重编码到 1080p/1200k+128k → 33.4MB，已落地 public/projects/ 与 dist/。
- 未恢复：moon-inn-ep2.mp4、secret-heir.mp4 原始文件磁盘无备份（微信缓存为哈希名无法可靠匹配），需用户重新发送或从原来源复制。
- 重新 `npm run build` 通过(3.93s)，dist 干净（three 542KB/gz136KB 等独立 chunk）；dev server 重启于 http://localhost:5173/（task Mt4w0M）。
